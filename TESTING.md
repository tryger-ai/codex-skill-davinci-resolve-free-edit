# 検証記録と再現手順

## 実施済み（2026-09-16）

Windows、Codex Desktop、Windows用Computer Use、DaVinci Resolve 20の日本語UIで実施。厳密なResolveビルド番号、Windowsビルド、Codex版は今回の記録では未採録です。これらの版全般を検証済みとは扱いません。

私有素材1本を使用し、空のタイムラインと空の標準Text+から日本語5区間・英語4区間を作成しました。元タイトルは参照のみで、設定のコピー元には使用していません。作成した自身のタイトルを後続区間に複製しています。

| 項目 | 結果 |
|---|---|
| 素材配置・新規タイムライン | 実施済み |
| Text+本文・日英書式・配置・表示時間 | 実施済み、全字幕の代表フレームを確認 |
| プロジェクト保存 | UI保存済み。再起動後の再読込は未検証 |
| MP4書き出し | 完了表示と実ファイルを確認 |
| 出力形式 | H.264、1080×1920、24fps、AAC |
| 映像の全デコード | 361フレーム、エラーなし |
| 音声の全デコード | 706音声フレーム、722944サンプル、エラーなし |
| ファイル | 29,510,840 bytes、コンテナ長15.061333秒 |
| 音声の直接聴取 | 未実施。音質・自然さ・知覚上の同期は未保証 |

上記は既存作業で得られた結果であり、公開準備中に全編集を再実行したものではありません。出力動画・画像・素材は公開許諾を得ておらず同梱しません。構文検証はGUI機能の検証ではありません。

## 未検証の操作

複数素材の切り出しと連結、映像のみ／別素材の音声のみの配置、一定速度・逆再生・静止・速度ランプ、SRT読み込み、映像・音声クロスフェード、プロジェクト再起動後の保持、別PCでの導入。

## 次回の小規模な実機テスト

自身で作成・利用許諾を持つ短い素材A・Bを使用し、既存制作案件から独立した新規プロジェクトで実施します。素材は各10秒以上、同じ24fpsで、目視できる時刻と音声の目印があるものを推奨します。素材がない場合はテストを実施済みにしません。

| テスト | 操作 | 合格条件 |
|---|---|---|
| T1 切り出しと連結 | Aの[1秒,3秒)、Bの[4秒,7秒)を連結 | 合計120フレーム。指定順、隙間・重複なし。境界前後も正しい |
| T2 映像と音声の別配置 | Aの映像[1秒,4秒)とBの音声[4秒,7秒) | 合計72フレーム。A音声とB映像が混入しない。音声の開始位置が指定どおり |
| T3 二言語テロップ | T2に指定日本語と英訳を追加 | 文字・位置・開始終了を確認。はみ出しなし |
| T4 保存と納品 | 保存、MP4出力、プロジェクトを開き直す | 編集内容保持。映像・音声の読み込みと字幕表示が正常 |

区間は終了を含まない表記です。ResolveのOut点の包含規則はUIのDurationと書き出しフレーム数で照合します。音声の検査は直接聴取の有無を明記します。

各テストには実施日、各アプリの版、素材仕様、期待値、実測値、合否、残る問題を追記します。未検証の機能は、対応手順があるという理由だけで合格へ変更しません。
